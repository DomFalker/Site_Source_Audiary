services:
  - type: web
    name: siteaudiario
    plan: free
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: siteaudiariodb
          property: connectionString
      - key: PYTHON_VERSION
        value: 3.12.0

databases:
  - name: siteaudiariodb
    plan: free
    databaseName: audiary
